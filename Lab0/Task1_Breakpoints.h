#pragma once

// Задание 1 (раздел "Алгоритмы и отладка", методичка стр. 4-5).
//
// Смысл задания - не написать что-то новое, а руками потрогать отладчик:
// поставить точку останова, пройтись по циклу F10/F5, посмотреть на значения
// переменных, а во втором примере - задать точке останова условие (i == 777).
// Сами наблюдения (какие значения принимает sum) фиксируются отдельно
// в отчёте/тетради, а не в коде.

// Первый пример: обычная точка останова внутри цикла на 10 итераций (стр. 4).
void Task1_Breakpoints();

// Второй пример: условная точка останова, срабатывающая при i == 777 (стр. 5).
void Task1_ConditionalBreakpoint();
