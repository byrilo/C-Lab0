#include "Task2_Arrays.h"
#include "Common.h"
#include <iostream>

using namespace std;

void Task2_SortIntArray()
{
    // TODO:
    // 1. int array[10] = { ... };  - придумайте свои 10 значений (можно с отрицательными).
    // 2. Вывести "Source array is:" и сам массив (PrintIntArray).
    // 3. Вызвать SortIntArray(array, 10).
    // 4. Вывести "Sorted array is:" и отсортированный массив.
}

void Task2_CountAboveValue()
{
    // TODO:
    // 1. double array[12] = { ... };
    // 2. Вывести "Source array is:" и массив (PrintDoubleArray).
    // 3. cout << "Enter searching value: "; cin >> searchingValue;
    // 4. int count = CountAboveOrEqual(array, 12, searchingValue);
    // 5. Вывести "Elements of array more than X is: " << count.
}

void Task2_FilterLetters()
{
    // TODO:
    // 1. char array[8];
    // 2. Цикл for: "Enter array of 8 chars", затем "a[i]: " и cin >> array[i] для каждого i.
    // 3. Вывести "Your array is:" и массив.
    // 4. Пройтись по массиву ещё раз, вывести "All letters in your array:"
    //    и только те символы, что являются буквами (a-z ИЛИ A-Z - см. пример в методичке,
    //    там регистр не игнорируется, обе буквы разного регистра проходят фильтр).
}
