#pragma once

// Задание 2 (раздел "Массивы", методичка стр. 6-7).
// Логика сортировки/подсчёта/фильтрации вынесена в Common.h, чтобы её же
// можно было переиспользовать в Задании 5 без дублирования кода.
// Здесь остаются только "сценарные" функции: создать массив, ввести/вывести
// его, вызвать нужную функцию из Common.h, показать результат.

// 2.1 Массив из 10 int, инициализация в коде, вывод, сортировка по возрастанию, вывод.
void Task2_SortIntArray();

// 2.2 Массив из 12 double, инициализация в коде, вывод, ввод searchingValue
//     с клавиатуры, подсчёт элементов >= searchingValue.
void Task2_CountAboveValue();

// 2.3 Массив из 8 char, ввод значений с клавиатуры (cin, по одному через цикл),
//     вывод исходного массива, затем вывод всех букв 'a'-'z'/'A'-'Z' с учётом регистра.
void Task2_FilterLetters();
