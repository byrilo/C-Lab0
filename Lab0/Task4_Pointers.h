#pragma once

// Задание 4 (раздел "Адреса и указатели", методичка стр. 11-17).
// Почти все примеры здесь даны в тексте дословно - задача не "решить",
// а переписать их у себя, скомпилировать и понаблюдать за адресами в выводе.
// Это дает возможность своими глазами увидеть разницу между
// значением/ссылкой/указателем на практике.

void Task4_AddressesOfVariables();     // адреса int/double/bool переменных, стр. 11
void Task4_AddressesOfArrayElements(); // адреса ячеек int[10] и double[10], стр. 11-12
void Task4_ReferenceDemo();            // int& b = a - ссылка меняет исходную переменную, стр. 12-13
void Task4_PassByValue();              // void Foo(double a) - копия, исходная не меняется, стр. 13
void Task4_PassByReference();          // void Foo(double& a) - меняет исходную, стр. 14
void Task4_PointerBasics();            // int* pointer = &a; разыменование *pointer, стр. 15
void Task4_PassByPointer();            // void Foo(double* a) - передача по указателю, стр. 16

// Письменный вопрос (не код, ответ - в отчёт): как в исходнике отличить
// разыменование, объявление указателя и умножение? Все три используют '*'.
